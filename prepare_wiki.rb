#//alert( "d:"+d+" m:"+m+" s:"+s );
lines = File.read('latlng-world.wiki').split("\n")
lines.each do |line|
  lat, lng, city, state, country = line.split(', ')
  p city
  sign = 
d = d-0;
m = m-0;
end
#var sign = ( dir=='W' || dir==alert'S' ) ? -1 : 1;
return (((s/60+m)/60)+d)*sign;sign
